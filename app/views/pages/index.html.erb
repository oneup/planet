<h1>Listing pages</h1>

<table>
  <tr>
  </tr>

<% @pages.each do |page| %>
  <tr>
    <td><%= link_to h(page.name), page %></td>
    <td class='dings'><%= link_to '!', edit_page_path(page) %></td>
    <td class='dings'><%= link_to 'M', page, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New page', new_page_path %>